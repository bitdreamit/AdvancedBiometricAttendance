{
  "_readme": "Advanced Biometric Attendance — Offline Agent v2.2",
  "_instructions": "Replace ALL placeholder values. See README.md for full guide.",

  "database": {
    "path": "data/att.db"
  },

  "logging": {
    "level": "INFO",
    "file": "logs/app.log",
    "max_size_mb": 10,
    "backup_count": 5
  },

  "devices": [
    {
      "_comment": "Set ip, serial_number, and enabled=true for your device",
      "ip": "192.168.1.201",
      "port": 4370,
      "serial_number": "YOUR_DEVICE_SERIAL",
      "name": "Main Entrance",
      "enabled": false,
      "timeout": 30,
      "sync_time": true
    }
  ],

  "server": {
    "_comment": "Set url and api_key to enable server sync. Leave url blank for offline-only.",
    "url": "",
    "api_key": "",
    "tenant_id": "",
    "sync_enabled": false,
    "verify_ssl": true,
    "timeout": 30
  },

  "sync": {
    "interval_seconds": 300,
    "batch_size": 100,
    "batch_interval": 300,
    "clear_after_batch": false
  },

  "agent_api": {
    "_comment": "HTTP status API — lets Laravel check agent health and push commands",
    "enabled": false,
    "host": "127.0.0.1",
    "port": 5000,
    "token": ""
  }
}
